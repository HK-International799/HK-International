import { useContext } from "react";
import { AuthContext } from "../contexts/AuthContext";

// Re-export for convenience — same as useAuth from AuthContext
export { useAuth } from "../contexts/AuthContext";
